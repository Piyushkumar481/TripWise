spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tripwise_test
    username: postgres
    password: Piyush@2004
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop

    show-sql: false

  sql:
    init:
      mode: never